#!/bin/bash 

alias c="clear"
alias q="exit"
alias s='sudo'
alias hosts='sudo vi /etc/hosts'
# alias rm="sudo rm -rfiv "

alias myip="curl -4 icanhazip.com"

# programs
alias v='sudo vim'
alias vi="echo 'use v'"
alias vim="echo 'use v'"

alias mysql='sudo mysql'
alias p='python3'
alias python='python3'

alias dots-origin="echo git remote add origin git@github.com:lukosevicius/dotfiles.git"


alias rmrf='sudo rm -rf'
# alias rmrf*='sudo rm -rf * && sudo rm -rf .*'

alias ldu='du -cks * | sort -rn | head -15' # Lists the size of all the folders
alias psg='ps aux | grep'


# php
alias phpinfo="echo '<?php phpinfo(); ?>' > index.php"

# Make cp and mv print info, what was done
alias cp="cp -iv"
alias mv="mv -iv"
alias ln="ln -iv"

# get

alias inc="mv /var/www/incoming/* ."

alias get-bedrock="wget https://github.com/roots/bedrock/archive/master.zip && unzip master.zip && rm master.zip && mv bedrock-master/*(DN) . && rm -rf bedrock-master/"
alias getlamp="wget --no-cache -O - https://gist.github.com/EmpireWorld/737fbb9f403d4dd66dee1364d866ba7e/raw/install-lamp.sh | bash"
alias getgravdeps="sudo apt-get install php7.4-mbstring php7.4-curl php7.4-zip php7.4-dom php7.4-gd"
# sudo apt composer install
# sudo a2enmod rewrite

# install
alias getaxios="npm install --save axios@0.18.1"

# git
# alias ga="git add ."
# alias gc="git commit"
# alias gp="git push"
